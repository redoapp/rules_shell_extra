load("//shell:rules.bzl", "sh_binary")

package(default_visibility = ["//visibility:public"])

sh_binary(
    name = "refresh",
    data = [
        "@files//packages:bazelrc",
    ],
    main = "refresh.sh",
)
