load("//shfmt:rules.bzl", "shfmt_format")

package(default_visibility = ["//visibility:public"])

shfmt_format(
    name = "shfmt",
    options = [
        "-ci",
        "-i",
        "4",
        "-s",
    ],
)
